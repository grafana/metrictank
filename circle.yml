test:
  post:
    - ./package.sh
dependencies:
  pre:
    - gem install fpm
    - gem install package_cloud
    - mkdir artifacts
general:
  artifacts:
    - artifacts
deployment:
  production:
    commands:
      - package_cloud push splaspood/raintank/ubuntu/trusty artifacts/*.deb
