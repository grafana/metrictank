machine:
  pre:
    - pkg/depends.sh
  post:
    - pkg/package.sh
general:
  artifacts:
    - pkg/artifacts
deployment:
  production:
    branch: circleci
    commands:
      - package_cloud push raintank/raintank-unstable/ubuntu/trusty artifacts/*.deb
