test:
  pre:
    - pkg/depends.sh
  post:
    - pkg/package.sh
dependencies:
  override:
    - pkg/build.sh
general:
  artifacts:
    - pkg/artifacts
deployment:
  production:
    branch: master
    commands:
      - pkg/deploy.sh
